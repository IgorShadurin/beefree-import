import React, {Component} from 'react';
import Layout from "./Layout";
import './App.css';

class App extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Layout/>
        );
    }
}

export default App;
